{
  "name": "cheguanjia-prototypes",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "dev": "npm run sync && vitepress dev .",
    "build": "vitepress build .",
    "prebuild": "node scripts/sync.mjs",
    "preview": "vitepress preview ."
  },
  "dependencies": {
    "vitepress": "^1.6.4",
    "vue": "^3.5.38",
    "ws": "^8.21.0"
  }
}
